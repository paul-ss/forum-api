database: {
  postgres: {
    driver_name: "postgres",
    username: "forum_user",
    password: "662f2710-4e08",
    db_name: "forum_db",
    ssl_mode: "disable",
    host: "localhost",
    max_conn: 10
  }
}

web: {
  server: {
    address: "0.0.0.0",
    port: "5000",
    host: "pinterest-tp.tk",
    protocol: "http",
  },
}


logger: {
  gin_file:  "/var/log/pinterest/gin.log",
  gin_level: "release",  # (release|test|debug)
  common_file: "/var/log/pinterest/common.log",
  common_level: "fatal",  # (panic|fatal|error|warn|info|debug|trace)
  stdout_log: "true",
}
